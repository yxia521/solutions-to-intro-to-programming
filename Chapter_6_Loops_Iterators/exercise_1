  This will return exactly [1, 2, 3, 4, 5] because .each method will return the original array.

  Pay attention to the dffierence between this and the following:
  x = [1, 2, 3, 4, 5]
  x.each{|a| puts a += 1}